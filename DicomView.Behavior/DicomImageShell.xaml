<UserControl x:Class="DicomView.Behaviors.DicomImageShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="shell" 
             DataContextChanged="shell_DataContextChanged" 
             Background="Black"
             VerticalAlignment="Center"
             HorizontalAlignment="Center"
             IsHitTestVisible="True">
    <Viewbox Stretch="Uniform">
        <Grid x:Name="triggerPanel" Background="Black">
            <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <ItemsControl HorizontalAlignment="Right" VerticalAlignment="Top"/>
            <ItemsControl HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
            <ItemsControl HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
            <Image Source="{Binding DicomImage}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <!--放置roi等-->
            <Canvas x:Name="canvas" Width="{Binding Cols}" Height="{Binding Rows}" ClipToBounds="True"/>
        </Grid>
    </Viewbox>
</UserControl>
